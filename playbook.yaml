---

- hosts: localhost
  vars:
    code_dir: "{{ ansible_user_dir ~ '/Workspace' }}"
    xdg_config_home: "{{ ansible_user_dir ~ '/.config' }}"
  roles:
    - role: dots
      tags:
        - "dotfiles"
